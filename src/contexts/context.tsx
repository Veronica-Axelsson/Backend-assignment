import {createContext} from 'react';


export const ProductContext = createContext<string | undefined>(undefined)
export const FeaturedProducts = createContext<string | undefined>(undefined)
// export const ProductSection1Context = createContext<string | undefined>(undefined)